spring:
  application:
    name: myclient
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG-SERVER
#      uri: "http://localhost:8888"
#      profile: dev
eureka:
  client:
    serviceUrl:
      defaultZone: "http://localhost:1111/eureka/"